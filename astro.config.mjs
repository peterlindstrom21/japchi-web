// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://jopchi.com',
  // No base path — CNAME maps the apex domain
});
