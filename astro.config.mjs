// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://japchi.com',
  // No base path — CNAME maps the apex domain
});
